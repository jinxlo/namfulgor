# Flask App Configuration
FLASK_APP=run.py
FLASK_ENV=development # Use 'production' for deployment
SECRET_KEY='secret' # Generate one: python -c 'import secrets; print(secrets.token_hex(16))'

# OpenAI API Configuration
OPENAI_API_KEY='apiKey' # Replace with your actual OpenAI API key
OPENAI_EMBEDDING_MODEL='text-embedding-3-small' # Or another model supported by the API version
OPENAI_CHAT_MODEL='gpt-4o-mini' # Or another model like gpt-4

# PostgreSQL Database Configuration
# Example: postgresql://user:password@hostname:port/database_name
# Ensure user has permissions on the database and pgvector is enabled
DATABASE_URL='postgres'

# WooCommerce API Configuration
WOOCOMMERCE_URL='url' # Your WooCommerce store URL (use https://)
WOOCOMMERCE_KEY='key' # Your WooCommerce API key
WOOCOMMERCE_SECRET='secret' # Your WooCommerce API secret
WOOCOMMERCE_API_VERSION='wc/v3' # Usually wc/v3
WOOCOMMERCE_TIMEOUT=30 # Timeout in seconds for WooComm API calls

# Scheduler settings
SYNC_INTERVAL_MINUTES=60 # How often to run the background sync (set to 0 to disable automatic sync)


# Other settings
LOG_LEVEL=DEBUG # Set log level (DEBUG, INFO, WARNING, ERROR)
EMBEDDING_DIMENSION=1536 # Dimension of OpenAI's text-embedding-3-small
PRODUCT_SEARCH_LIMIT=5 # Default number of products to return in searches


# --- Support Board API & Webhook Config ---
# URL to your SB installation's api.php 
SUPPORT_BOARD_API_URL="https://dashboard.nulu.chat/script/include/api.php"

# Get from SB Admin Area -> Users -> Find Admin User -> Copy Token
SUPPORT_BOARD_API_TOKEN="apiToken"

# --- User IDs for Sending Messages ---
# Define your user IDs here for easy reference
SUPPORT_BOARD_BOT_USER_ID_REF="2"      # The ID of your designated bot user
SUPPORT_BOARD_AGENT_USER_ID_REF="2" # The ID of your actual agent user

# *** Variable checked by api/routes.py (NEEDS TO BE PRESENT) ***
# Set this to the ID that makes sense for initial checks or fallbacks
# (e.g., the actual bot ID '2', even if non-functional for sending WA)
SUPPORT_BOARD_BOT_USER_ID="2"  # <--- ENSURE THIS LINE EXISTS AND HAS A VALUE

# *** Set the ACTIVE User ID that the WA sending function will use ***
# Change the value below to test sending as the agent ('3661') or bot ('2').
SUPPORT_BOARD_SENDER_USER_ID="1" # <--- THIS CONTROLS THE 'user' FOR WA SENDING

# Optional: Secret key you configure in SB Webhook settings for verification
SUPPORT_BOARD_WEBHOOK_SECRET="webhookSecretKey"


# --- WhatsApp Cloud API Credentials (NEW) ---
WHATSAPP_CLOUD_API_TOKEN="whatsAppCloudApiToken" # Replace with your actual token
WHATSAPP_PHONE_NUMBER_ID="129366486929481"
WHATSAPP_DEFAULT_COUNTRY_CODE="58"
WHATSAPP_API_VERSION="v19.0"

# --- Human Agent Takeover Configuration (NEW) ---
SUPPORT_BOARD_AGENT_IDS="1"   # Replace with YOUR comma-separated agent User IDs from Support Board
HUMAN_TAKEOVER_PAUSE_MINUTES="15" # Default pause duration in minutes